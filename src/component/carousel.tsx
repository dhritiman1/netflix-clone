import type { Content } from "@/types";

type Props = {
  title: string;
  content: Content[];
};
export const Carousel = ({ title, content }: Props) => {
  return (
    <div>
      <div></div>
    </div>
  );
};
